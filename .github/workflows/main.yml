name: Create a distribution package
on:
  push:
    branches:
      - master
  # workflow_call:
  #   inputs:
  #     testvar:
  #       description: 'test'
  #       default: 'main-default-value'
  #       required: true
  #       type: string

jobs:
  tests:
    runs-on: ubuntu-latest
    # env:
    #   BRANCHAQUAWEBNGVUE: ${{ github.event.inputs.testvar }}
    steps:        
      - name: Checkout test
        uses: actions/checkout@v2
        with:
          path: aquawebngvue
          ref: test-checkout-11

      - run: ls -la ./aquawebngvue