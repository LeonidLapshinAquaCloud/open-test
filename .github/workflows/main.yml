name: Create a distribution package
on:
  workflow_dispatch:
    inputs:
      frontend_branch:
        description: 'frontend branch'     
        required: true
        default: 'development'

jobs:
  tests:
    runs-on: windows-latest
    steps:
      - run: |
          echo ${{ env.INPUT_FRONTEND_BRANCH }}
