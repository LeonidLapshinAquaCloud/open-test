name: CI

on:
  push:
    branches: [ master, release* ]

jobs:
  tests:
    runs-on: windows-latest
    env:
      AQUA_APP_VERSION: "0.0"
    steps:
      - uses: octokit/request-action@v2.x
        id: get_all_repo_tags
        with:
          route: GET /repos/{owner}/{repo}/git/matching-refs/tags
          owner: LeonidLapshinAquaCloud
          repo: open-test
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          echo '${{ steps.get_all_repo_tags.outputs.data }}' > full_tag_list.txt
          echo "==="
          cat full_tag_list.txt | jq '.[].ref' | ForEach-Object {$_ -replace '"',''} 
          echo "==="
          cat full_tag_list.txt | jq '.[].ref' | ForEach-Object {$_ -replace '"',''} | cut -d '/' -f 3 
          echo "==="
          cat full_tag_list.txt | jq '.[].ref' | ForEach-Object {$_ -replace '"',''} | cut -d '/' -f 3 | Select-String -Pattern "refs/tags/v${{ env.AQUA_APP_VERSION }}"
        if: contains(github.ref, 'release')

      # - name: Bump version and push tag
      #   id: tag_version
      #   uses: mathieudutour/github-tag-action@v5.6
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      # - run: "echo latest tags: ${{ steps.tag_version.outputs.new_tag }}"

      # - name: Create a GitHub release
      #   uses: ncipollo/release-action@v1
      #   with:
      #     tag: ${{ steps.tag_version.outputs.new_tag }}
      #     name: Release ${{ steps.tag_version.outputs.new_tag }}
      #     body: ${{ steps.tag_version.outputs.changelog }}