name: Create a distribution package
on:
  # push:
  #   branches:
  #     - master
  workflow_call:
  #   inputs:
  #     testvar:
  #       description: 'test'
  #       default: 'main-default-value'
  #       required: true
  #       type: string

jobs:
  tests:
    runs-on: ubuntu-latest
    # env:
    #   BRANCHAQUAWEBNGVUE: ${{ github.event.inputs.testvar }}
    steps:        
      - name: Create artifact with version number 
        run: |
          echo "444" > tag.txt
        shell: pwsh

      - name: Create artifact with version number 
        uses: actions/upload-artifact@v2
        with:
          name: tag
          path: tag.txt