name: Create a distribution package
on:
  push:
    branches:
      - master

env:
  TAG: "2.2.14"

jobs:
  tests:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2.3.4
        with:
          path: source

      - name: getVersion
        run: |
          if ("${{ github.ref }}" -match "master") {echo "dsfsd"}
