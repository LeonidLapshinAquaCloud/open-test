name: Create a distribution package
on:
  # push:
  #   branches:
  #     - master
  workflow_call:
    inputs:
      testvar:
        description: 'test'
        default: 'main-default-value'
        required: true
        type: string

jobs:
  tests:
    runs-on: windows-latest
    env:
      BRANCHAQUAWEBNGVUE: ${{ github.event.inputs.INPUT_FRONTEND_BRANCH }}
    steps:        
      - run: echo "${{ env.BRANCHAQUAWEBNGVUE }}"